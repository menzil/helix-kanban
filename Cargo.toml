[package]
name = "kanban"
version = "0.2.0"
edition = "2021"

[dependencies]
# TUI framework
ratatui = "0.28"
crossterm = "0.28"

# Data serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"

# Utilities
chrono = "0.4"
directories = "5.0"
anyhow = "1.0"
